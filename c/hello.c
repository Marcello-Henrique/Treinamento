#include <stdio.h>      // Importa a biblioteca padrão de entrada/saída (printf)

int main() {            // Função principal: ponto de entrada do programa em C
    printf("Hello, C!\n");  // Imprime texto no console e quebra a linha (\n)
    return 0;           // Retorna 0 indicando que terminou sem erros
}
